diff --git a/lib/Drivers/DML/mysql.js b/lib/Drivers/DML/mysql.js
index 095b3f4..5961137 100644
--- a/lib/Drivers/DML/mysql.js
+++ b/lib/Drivers/DML/mysql.js
@@ -46,7 +46,7 @@ Driver.prototype.on = function (ev, cb) {
 
 Driver.prototype.connect = function (cb) {
   if (this.opts.pool) {
-    return this.db.pool.getConnection(function (err, con) {
+    this.db.getConnection(function (err, con) {
       if (!err) {
         if (con.release) {
           con.release();
@@ -56,8 +56,9 @@ Driver.prototype.connect = function (cb) {
       }
       return cb(err);
     });
+  } else {
+    this.db.connect(cb);
   }
-  this.db.connect(cb);
 };
 
 Driver.prototype.reconnect = function (cb, connection) {
@@ -71,21 +72,23 @@ Driver.prototype.reconnect = function (cb, connection) {
     connOpts = connOpts.replace("debug=true", "debug=false");
   }
 
-  this.db = (connection ? connection : mysql.createConnection(connOpts));
-  if (this.opts.pool) {
-    this.db.pool = (connection ? connection : mysql.createPool(connOpts));
+  if (connection) {
+    this.db = connection;
+  } else {
+    if (this.opts.pool) {
+      this.db = mysql.createPool(connOpts);
+    } else {
+      this.db = mysql.createConnection(connOpts);
+    }
   }
+
   if (typeof cb == "function") {
     this.connect(cb);
   }
 };
 
 Driver.prototype.close = function (cb) {
-  if (this.opts.pool) {
-    this.db.pool.end(cb);
-  } else {
-    this.db.end(cb);
-  }
+  this.db.end(cb);
 };
 
 Driver.prototype.getQuery = function () {
@@ -223,7 +226,7 @@ Driver.prototype.clear = function (table, cb) {
 };
 
 Driver.prototype.poolQuery = function (query, cb) {
-  this.db.pool.getConnection(function (err, con) {
+  this.db.getConnection(function (err, con) {
     if (err) {
       return cb(err);
     }
diff --git a/test/integration/orm-exports.js b/test/integration/orm-exports.js
index 4fe55ea..6a64815 100644
--- a/test/integration/orm-exports.js
+++ b/test/integration/orm-exports.js
@@ -177,7 +177,6 @@ describe("ORM", function() {
 
         it("should understand pool `'true'` from query string", function () {
           var connString = connStr + "debug=true&pool=true";
-          console.log("#########", connString, connStr, Buffer.from(connString).toString('base64'));
           return ORM.connectAsync(connString)
             .then(function (db) {
               should.strictEqual(db.driver.opts.pool,  true);
@@ -359,7 +358,7 @@ describe("ORM", function() {
           JSON.stringify(opts),
           expected
         );
-        db.close(done);
+        done();
       });
     });
 
